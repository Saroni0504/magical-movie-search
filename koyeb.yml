name: disney-search-engine
runtime: python
type: web
path: /app/main.py
build:
  commands:
    - pip install -r requirements.txt
routes:
  - path: /
    methods:
      - GET
healthcheck:
  type: http
  path: /
  interval: 5s
  timeout: 3s
